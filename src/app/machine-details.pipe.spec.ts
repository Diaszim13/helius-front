import { MachineDetailsPipe } from './machine-details.pipe';

describe('MachineDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new MachineDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
